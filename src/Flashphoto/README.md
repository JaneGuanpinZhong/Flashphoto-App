A graphic user interface, which uses tools and filters from imagetools library.

There is only one major class actually used to manipulate Flashphoto application.

image_tools::FlashPhotoApp is the FlashPhoto GUI, a graphics application class. This class creates 
a graphics window to display the current PixelBuffer and a graphical user interface to interact with
it using Tools and Filters.
